pipeline {
     agent any 
     stages {
         stage('Testing') {
         steps {
              sh '''
               
               mkdir saadi
               ls
               [ -d "/saadi" ] && echo "Folder exist"
               
              

              '''
          }
        }
    }         
}
