dpipeline {
     agent any
     
     
     stages {
         stage('Submit Stack') {
         steps {
              sh "echo ${VpcCIDR}"
           }
          }
         }         
         }
