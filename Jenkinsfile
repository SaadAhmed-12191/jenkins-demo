pipeline {
     agent any 
     stages {
         stage('Testing') {
         steps {
              sh '''
               
              
               [ -d "/saadi" ] && echo "Folder exist"
               
              

              '''
          }
        }
    }         
}
